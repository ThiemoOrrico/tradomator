import {createTheme} from "@mui/material";

const themeAppBar = createTheme({

        props: {
            MuiAppBar: {
                color: 'secondary',
            },
        },

}
);

export default themeAppBar;